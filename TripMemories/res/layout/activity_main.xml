<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:background="@drawable/fundal"
    android:orientation="vertical" >

    <Button
        android:id="@+id/alDoileaButon"
        android:layout_width="250dp"
        android:layout_height="wrap_content"
        android:layout_above="@+id/PrimuButon"
        android:layout_alignLeft="@+id/PrimuButon"
        android:layout_marginBottom="17dp"
        android:text="@string/camera"
        android:textColor="#0000BB"
        android:textStyle="bold" />

    <Button
        android:id="@+id/PrimuButon"
        android:layout_width="250dp"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="86dp"
        android:text="@string/galerie"
        android:textColor="#0000CC"
        android:textStyle="bold" />
     
</RelativeLayout>